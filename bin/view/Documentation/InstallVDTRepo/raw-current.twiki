%LINKCSS%

&lt;!-- This is the default OSG documentation template. Please modify it in --&gt;
&lt;!-- the sections indicated to create your topic.                        --&gt; 

&lt;!-- By default the title is the WikiWord used to create this topic. If  --&gt;
&lt;!-- you want to modify it to something more meaningful, just replace    --&gt;
&lt;!-- %TOPIC% below with i.e &quot;My Topic&quot;.                                  --&gt;

---+!! Install the OSG Yum Repository

---+!! %RED% This document is deprecated, please refer to Documentation.Release3.YumRepositories instead !!%ENDCOLOR%

%TOC%

---+++ Repo Requirements

%STARTINCLUDE%

The OSG RPMs currently support Red Hat Enterprise Linux 5 and variants (Scientific Linux 5 and !CentOS 5). Currently most of our testing has been done on Scientific Linux 5: your mileage may vary.

OSG RPMs are distributed via the OSG yum repositories. Some packages depend on packages distributed via the [[http://fedoraproject.org/wiki/EPEL][EPEL]] repositories. So both repositories must be enabled.
   1. If not already present, install EPEL: &lt;pre class=&quot;rootscreen&quot;&gt;
%UCL_PROMPT_ROOT% wget http://download.fedoraproject.org/pub/epel/5/i386/epel-release-5-4.noarch.rpm
%UCL_PROMPT_ROOT% rpm -i epel-release-5-4.noarch.rpm &lt;/pre&gt;
   1. Install the =yum-priorities= plugin: &lt;pre class=&quot;rootscreen&quot;&gt;
%UCL_PROMPT_ROOT% yum install yum-priorities&lt;/pre&gt;
   1. Install the OSG repositories: &lt;pre class=&quot;rootscreen&quot;&gt;
%UCL_PROMPT_ROOT% rpm -Uvh http://repo.grid.iu.edu/osg-release-latest.rpm&lt;/pre&gt;
       *WARNING*: if you have your own mirror or configuration of the EPEL repository, you *MUST* verify that the OSG repository has a better yum priority than EPEL.  Otherwise, you will have strange depsolving issues.%BR%
       *COMMENT: Please provide detailed syntax for how to do this. -Horst*

%STOPINCLUDE%
